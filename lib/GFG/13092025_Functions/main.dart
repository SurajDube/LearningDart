main(List<String> arguments){
     //printing the arguments along with length
     print(arguments.length);
     print(arguments);

     // using terminal run the following command.
     // dart fileName.dart arguments1,arguments2,arguments3
}